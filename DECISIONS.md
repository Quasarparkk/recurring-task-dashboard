# 설계 결정 기록 (DECISIONS)

이 문서는 개발 중 내린 설계 결정과 **그 이유**, 그리고 **검토했지만 채택하지 않은 대안**을 기록합니다.
구현을 읽어도 알 수 없는 "왜"를 남기는 것이 목적입니다.

---

## D-001. 업무 정의(Task)와 발생 건(Occurrence)의 분리

**결정**: 반복 규칙을 가진 템플릿(`Task`)과 실제 실행 단위(`Occurrence`)를 별도 테이블로 분리한다.
완료 여부, 담당자, 메모, 체크리스트는 모두 `Occurrence` 에 기록한다.

**이유**:
- 일반 To-Do 앱처럼 하나의 레코드에 `dueDate` + `isDone` 을 두면, 반복 업무의 과거 이력을 남길 수 없다.
- "작년 연말정산 취합은 누가 했고 언제 끝냈나"는 이 도구의 핵심 가치인데, 분리 없이는 답할 수 없다.
- 반복 규칙 수정 시 과거 기록이 함께 변형되는 사고를 구조적으로 막을 수 있다.

**과거 이력 보존 규칙**: 롤링 생성 배치는 `status ∈ (DONE, SKIPPED)` 이거나 마감일이 이미 지난
Occurrence 를 절대 삭제·수정하지 않는다. 규칙 변경은 **미래 회차에만** 반영된다.

---

## D-002. 반복 규칙 표현 — RRULE 문자열 대신 자체 JSON 판별 유니온

**결정**: RFC 5545 RRULE 문자열을 쓰지 않고, `type` 을 판별자로 갖는 자체 JSON 스키마를 정의해
`Task.recurrenceConfig` 에 직렬화 저장한다. 검증은 zod 로 한다.
(스키마 정의: [`src/lib/recurrence/types.ts`](src/lib/recurrence/types.ts))

**이유**: 요구사항 중 다음 항목들은 RRULE 에 표현할 표준 필드가 없다.

| 요구사항 | RRULE 로 가능한가 |
|---|---|
| 분기 종료 후 10**영업일** | 불가 — 영업일 개념이 없음 |
| 마감일이 공휴일이면 앞당김/미룸 | 불가 — 대체 규칙 필드가 없음 |
| 회계연도 시작월이 4월인 분기 | `BYMONTH` 조합으로 우회해야 함 |
| 특정 회차만 날짜 변경 | `RDATE`/`EXDATE` 로 가능하지만 원본 회차와의 대응 관계가 소실됨 |
| "매월 31일"이 없는 달의 처리 정책 | 불가 — 구현체마다 동작이 다름 |

RRULE 을 쓰면 위 항목을 결국 별도 컬럼으로 빼야 하므로, **표준을 따르는 이득 없이
파싱·역파싱 계층만 추가**된다. 반면 자체 JSON 은:

1. UI 폼 입력 항목과 스키마 필드가 1:1 대응 → "다음 10회 미리보기" 구현이 단순
2. TypeScript 가 `type` 으로 필드를 좁혀줌 → 계산 로직에서 필드 오용 방지
3. zod 런타임 검증 → 잘못된 규칙이 DB 에 저장되지 않음

**대안 검토**: `rrule` npm 패키지 사용 → 위 5개 요구사항을 모두 래핑 코드로 감싸야 하고,
그 래핑이 결국 자체 스키마가 된다. 이중 표현이 되어 기각.

**향후 확장**: Google/Outlook 캘린더 연동이 필요해지면 자체 스키마 → RRULE **단방향 변환기**를
어댑터로 추가한다. 대부분의 규칙은 손실 없이 변환 가능하며, 영업일·공휴일 규칙은
변환 시점에 구체 날짜(`RDATE`)로 전개하면 된다.

---

## D-003. 지원하는 반복 규칙 종류

| type | 의미 | 예시 |
|---|---|---|
| `ONCE` | 1회성 | 2026-05-20 |
| `YEARLY` | 매년 특정 월/일 (격년 지원) | 매년 1월 31일 |
| `MONTHLY_DAY` | 매월 N일 / 말일 (격월·N개월 지원) | 매월 5일, 매월 말일 |
| `MONTHLY_NTH_WEEKDAY` | 매월 N번째 요일 | 매월 셋째 주 화요일 |
| `SPECIFIC_MONTHS_DAY` | 특정 월 복수 지정 + N일 | 3·6·9·12월 15일 |
| `SPECIFIC_MONTHS_NTH_WEEKDAY` | 특정 월 복수 지정 + N번째 요일 | 3·9월 첫째 주 월요일 |
| `QUARTERLY` | 분기 시작/종료 기준 + 오프셋 | 분기 종료 후 10영업일 |
| `WEEKLY` | 매주 / 격주 / N주마다 (요일 복수) | 매주 월·수·금 |
| `EVERY_N_DAYS` | N일마다 | 10일마다 |

요구사항의 "매분기 결산"은 `QUARTERLY` 로도, `MONTHLY_DAY(interval: 3)` 로도 표현할 수 있다.
전자는 회계 분기 개념이 필요할 때, 후자는 단순히 3개월 주기일 때 쓴다.

---

## D-004. 날짜 저장 규약 — "달력 날짜"와 "실제 시각"의 분리

**결정**: 두 종류의 시간을 명확히 구분한다.

| 종류 | 대상 | 저장 방식 | 다루는 모듈 |
|---|---|---|---|
| 달력 날짜 | `scheduledDate`, `originalDate`, `Holiday.date` | `"YYYY-MM-DD"` (PlainDate) / DB 는 UTC 자정 `DateTime` | [`plain-date.ts`](src/lib/date/plain-date.ts) |
| 실제 시각 | `completedAt`, `sentAt`, `plannedAt` | UTC 타임스탬프 | [`kst.ts`](src/lib/date/kst.ts) |

**이유**: "2026-03-05 마감"은 시각 개념이 없는 달력상의 날짜다. 이를 `Date` 로 다루면
실행 환경의 로컬 타임존에 따라 하루가 밀리는 버그가 **반드시** 발생한다.
(개발자 PC, CI 서버, 배포 서버의 타임존이 모두 다를 수 있다)

**검증 방법**: `vitest.config.ts` 에서 `TZ=America/Los_Angeles` (KST와 17시간 차)를 강제한다.
이 상태에서 138개 테스트가 통과하므로 타임존 누출이 없음이 보장된다.
KST 환경에서만 테스트하면 이 클래스의 버그를 절대 잡을 수 없다.

**대안 검토**:
- Temporal 폴리필 (`PlainDate` 타입 내장) → 아직 실험적이고 번들 크기가 크다. API 안정성 미보장.
- `date-fns-tz` 의 `zonedTimeToUtc` → 모든 계산에 타임존 인자를 넘겨야 해 실수하기 쉽다.
- **채택**: `date-fns` + `@date-fns/utc` 의 `UTCDate`(달력 계산) + `@date-fns/tz` 의 `TZDate`(시각 변환).
  `UTCDate` 는 모든 getter 가 UTC 기준이라 로컬 타임존이 계산에 개입할 수 없다.

---

## D-005. 공휴일 데이터는 코드가 아니라 데이터

**결정**: 공휴일은 `Holiday` 테이블에 저장하고, `data/holidays/<연도>.json` 에서 시드한다.
계산 로직(`business-day.ts`)은 공휴일을 **주입받기만 하고 직접 알지 못한다**.

**이유**:
- 설날·추석·석가탄신일은 음력 기반이라 양력 날짜를 계산으로 유도할 수 없다.
- 대체공휴일 규칙은 법 개정으로 바뀐다 (2021년, 2023년에 각각 확대됨).
- 임시공휴일은 정부가 그 해에 지정한다 (예측 불가).

따라서 하드코딩은 매년 코드 수정을 강제하고, 잘못된 계산식은 조용히 틀린 결과를 낸다.
데이터로 분리하면 비개발자도 JSON 을 갱신할 수 있다.

**추가 결정**: `Holiday.type` 에 `COMPANY` 를 두어 사내 휴무일(창립기념일 등)도 같은 방식으로
영업일 계산에 반영한다.

---

## D-006. 파생 상태(BLOCKED / OVERDUE)는 저장하지 않는다

**결정**: `Occurrence.status` 에는 `PENDING | IN_PROGRESS | DONE | SKIPPED` 만 저장한다.
`BLOCKED`(선행 미완료)와 `OVERDUE`(마감 초과)는 조회 시 계산한다.

**이유**:
- `OVERDUE` 는 "오늘"에 따라 달라진다. 저장하면 날짜가 바뀌는 순간 낡은 값이 되고,
  이를 갱신하려면 매일 전체 레코드를 UPDATE 하는 배치가 필요해진다.
- `BLOCKED` 는 선행 업무의 완료 여부에 따라 달라진다. 선행을 완료 처리할 때
  모든 후행을 연쇄 UPDATE 해야 하고, 의존 그래프가 깊어지면 정합성이 깨지기 쉽다.
- 계산 비용은 낮다 (인메모리 판정). 반면 저장 시의 정합성 리스크는 높다.

**결과**: 저장 상태와 표시 상태를 별도 타입으로 둔다.
(`OccurrenceStatus` vs `DerivedOccurrenceStatus` — [`src/lib/dependency/status.ts`](src/lib/dependency/status.ts))

---

## D-007. 회차 번호(sequenceIndex)의 불변성

**결정**: `sequenceIndex` 는 항상 `config.startDate` 부터 0번으로 시작해 순차 부여한다.
조회 범위를 좁혀도 번호는 달라지지 않는다. `SKIP` 예외로 제외된 회차도 번호를 소비한다.

**이유**: 의존관계의 `SAME_SEQUENCE` 매칭과 `maxOccurrences` 판정이 이 불변성에 의존한다.
`SKIP` 이 번호를 소비하지 않으면, 예외를 추가/제거할 때마다 이후 모든 회차의 번호가 밀려
이미 저장된 Occurrence 와 어긋난다.

---

## D-008. 1회성 예외는 회차 번호가 아니라 "원본 날짜"로 식별한다

**결정**: `RecurrenceException` 의 키는 `originalDate`(반복 규칙이 산출한 원본 날짜)다.

**이유**: 회차 번호를 키로 쓰면 반복 규칙을 수정했을 때 번호의 의미가 어긋나
**엉뚱한 회차에 예외가 적용된다**. 원본 날짜를 키로 쓰면, 규칙 변경으로 그 날짜가
더는 생성되지 않을 경우 예외가 그냥 무효화된다 — 즉 조용히 잘못 적용되는 대신
아무 일도 일어나지 않는다. 실패 모드가 훨씬 안전하다.

---

## D-009. "매월 31일"처럼 존재하지 않는 날짜의 처리

**결정**: `onMissingDay` 옵션으로 사용자가 선택한다.
- `CLAMP` (기본): 그 달의 말일로 당김 (2월 → 28/29일)
- `SKIP`: 그 달은 건너뜀

**이유**: 정답이 업무 성격에 따라 다르다. "매월 31일 마감"은 2월에도 마감해야 하므로 `CLAMP`,
"31일에만 실행하는 특수 점검"은 `SKIP` 이 맞다. 임의로 하나를 고르면 반대 케이스를 표현할 수 없다.

`MONTHLY_NTH_WEEKDAY` 의 `nth: 5` 가 없는 달에도 같은 옵션이 적용된다
(`CLAMP` → 마지막 해당 요일, `SKIP` → 건너뜀).

---

## D-010. 공휴일 이동 정책과 이동 대상 분리

**결정**: 두 가지 축을 별도 필드로 둔다.
- `holidayPolicy`: `KEEP` | `PREV_BUSINESS_DAY` | `NEXT_BUSINESS_DAY` (어디로 이동할지)
- `shiftTarget`: `WEEKEND_AND_HOLIDAY` | `WEEKEND_ONLY` | `HOLIDAY_ONLY` (무엇을 피할지)

**이유**: "주말은 피하되 공휴일에는 그대로 마감"하는 업무가 실제로 존재한다
(예: 시스템 자동 배치는 공휴일에도 돌아간다). 두 축을 하나로 합치면 조합을 표현할 수 없다.

**추가 결정**: `RESCHEDULE` 예외로 지정된 날짜에는 공휴일 보정을 **적용하지 않는다**.
사용자가 명시적으로 그 날짜를 지정했으므로 시스템이 다시 옮기면 의도를 무시하는 것이 된다.

---

## D-011. 조회 범위 판정 기준 (`boundBy`)

**결정**: `generateOccurrences` 는 `boundBy: "ORIGINAL" | "SCHEDULED"` 옵션을 받는다.

**이유**: 공휴일 보정으로 마감일이 월/연 경계를 넘을 수 있어, 두 기준이 실제로 다른 결과를 낸다.
- **저장**(롤링 생성)에는 `ORIGINAL` — 구간 경계에서 누락도 중복도 발생하지 않는다.
- **표시**(달력/미리보기)에는 `SCHEDULED` — 사용자가 실제로 보는 날짜가 기준이어야 한다.

구체적 예: 매월 1일 마감 + 앞당김 정책일 때, 2월 1일(일) 회차는 1월 30일(금)로 앞당겨진다.
"1월 달력에 표시할 항목"을 고르려면 `SCHEDULED` 기준이어야 이 건이 잡힌다.

---

## D-012. 분기 규칙에서 직전 분기 마감이 포함되는 동작

**결정**: `QUARTERLY` + `anchor: END` + 양수 오프셋인 경우, **직전 분기의 마감일이
시작일 이후라면 정상적으로 생성한다**.

**이유**: "분기 종료 후 10영업일"은 본질적으로 마감일이 다음 분기에 온다.
2026-01-01 부터 시작하는 업무라면 2025 Q4 결산(2026-01-15 마감)이 첫 건이어야 한다.
이 동작이 없으면 **연초에 전분기 결산 업무가 통째로 누락된다**.

구현상 분기 순회를 시작일이 속한 분기보다 2분기 앞에서 시작해 이를 보장한다.

---

## D-013. 의존관계는 Task 레벨 + Occurrence 레벨 오버라이드

**결정**: 두 계층을 모두 제공한다.
- `TaskDependency` (템플릿 레벨): 반복되는 업무 간 상시 선후 관계. 회차 매칭은 `matchStrategy` 로 결정.
- `OccurrenceDependencyOverride` (회차 레벨): 특정 회차끼리 직접 연결. 자동 매칭보다 우선.

**회차 매칭 전략**:
| 값 | 동작 | 적합한 경우 |
|---|---|---|
| `NEAREST_PRECEDING` (기본) | 후행 예정일 이전(또는 같은 날) 중 가장 가까운 선행 회차 | 주기가 다를 때. 가장 안전한 기본값 |
| `SAME_SEQUENCE` | 같은 회차 번호끼리 | 두 업무의 주기가 완전히 동일할 때 |
| `SAME_PERIOD` | 같은 달에 속한 선행 회차 | 월 단위로 묶이는 업무 |

**기본값을 `NEAREST_PRECEDING` 으로 둔 이유**: `SAME_SEQUENCE` 는 주기가 같을 때만 옳고,
주기가 다르면 (예: 월간 → 분기) 엉뚱한 회차와 짝지어진다. 반면 `NEAREST_PRECEDING` 은
주기가 같을 때도 대부분 올바른 결과를 내므로 실수 여지가 적다.

**추가 결정**: `isBlocking: false` 를 두어 "관계만 시각화하고 상태 계산에는 반영하지 않는"
참고용 연결을 표현할 수 있게 한다.

---

## D-014. 순환 참조는 등록 시점에 차단

**결정**: `TaskDependency` 생성/수정 시 DFS 로 순환을 탐지하고, 순환이 생기면
**경로를 포함한 에러 메시지**와 함께 거부한다. (예: `A → B → C → A`)

**이유**: 순환을 허용하면 blocked 상태 계산이 무한 루프에 빠지고, 사용자는 왜 업무가
영원히 대기 상태인지 알 수 없다. 등록 시점에 막으면 원인이 명확한 지점에서 실패한다.

읽기 경로(상태 계산, 그래프 렌더링)에도 방어적으로 방문 집합을 두어,
데이터가 어떤 이유로든 순환을 갖게 되더라도 무한 루프에 빠지지 않게 한다.

---

## D-015. 알림 스케줄링 — 사전 materialize 대신 매 틱 계산

**결정**: 발송할 알림을 미리 테이블에 만들어 두지 않는다. 스케줄러가 매 틱마다
"미완료 Occurrence × 알림 규칙"으로 발송 예정 시각을 계산하고,
`NotificationLog.dedupeKey` 유니크 제약으로 중복 발송을 막는다.

**이유**:
- 서버 재시작 후 누락 없는 재개가 **구조적으로** 보장된다. 놓친 알림은 다음 틱에
  "예정 시각이 이미 지났고 로그에 없는 알림"으로 자동 발견된다.
  별도의 복구 로직이 필요 없다.
- 알림 규칙이나 마감일이 수정될 때 materialize 된 레코드를 재생성/정리할 필요가 없다.
  (이 재생성 로직이 실무에서 버그의 주된 원천이다)

**보완**: 서버가 오래 정지했을 때 낡은 알림이 폭주하는 것을 막기 위해
`staleNotificationHours`(기본 48시간)를 넘긴 알림은 발송하지 않고
`status: SKIPPED_STALE` 로 로그만 남긴다. 왜 안 왔는지 추적 가능해야 하므로 로그는 남긴다.

**대안 검토**: `NotificationSchedule` 테이블에 materialize → 다가올 알림을 조회하기 쉬운
장점이 있으나, 규칙 변경 시 재생성 로직이 필요하고 그 로직의 버그가 곧 알림 누락/중복이 된다.
"다가올 알림 조회"는 별도 미리보기 API 로 온디맨드 계산해 해결했다.

---

## D-016. 발송 채널은 어댑터 패턴으로 추상화

**결정**: `NotificationChannelAdapter` 인터페이스를 정의하고 레지스트리에 등록한다.
1차 구현: `WEB_PUSH`(브라우저 알림), `EMAIL`.

**이유**: Slack/Teams 추가 시 어댑터 파일 하나를 작성하고 레지스트리에 한 줄 등록하면
끝나야 한다. 스케줄러·중복방지·이력 저장 로직은 채널을 몰라야 한다.

**이메일 로컬 개발**: `EMAIL_TRANSPORT` 환경변수로 `console`(기본) / `file` / `smtp` 를 선택한다.
SMTP 설정 없이도 즉시 동작 확인이 가능해야 `npm install && npm run dev` 만으로 실행된다는
요구사항을 만족한다.

---

## D-017. Occurrence 롤링 생성 — 고정 기간 없이 자동 확장

**결정**: "지금부터 N개월치는 항상 존재한다"는 롤링 윈도우를 유지한다.
기본 18개월, `AppSetting.rollingWindowMonths` 또는 Task 별 `rollingWindowMonths` 로 조정.

**트리거 시점**: (a) 서버 시작 시, (b) 매일 새벽 배치, (c) Task 등록/수정 시 즉시.
`Task.generatedUntil` 워터마크로 "어디서부터 이어서 생성할지"를 기록한다.

**이유**: 사용자가 기간을 신경 쓰지 않아도 되게 한다는 요구사항을 만족하면서,
무한 생성으로 DB 가 비대해지는 것을 막는다. `endDate`/`maxOccurrences` 가 있는 규칙은
그 한도까지만 생성하고 `isRecurrenceExhausted` 로 완료를 판정한다.

---

## D-018. SQLite 로 시작하되 PostgreSQL 전환 가능하게

**결정**: 다음 제약을 지켜 스키마를 작성한다.
- Prisma `enum` 미사용 → `String` + 주석으로 허용값 명시 (SQLite 미지원)
- 스칼라 배열(`String[]`) 미사용 → 별도 모델(`Tag`) 또는 JSON 문자열 (SQLite 미지원)
- provider 별 native type(`@db.*`) 미사용

전환 시 `datasource.provider` 를 `"postgresql"` 로, `DATABASE_URL` 을 교체하면 된다.

**태그/카테고리를 JSON 대신 별도 모델로 둔 이유**: 담당자·카테고리·상태·중요도 필터가
요구사항에 있으므로 SQL 로 필터링 가능해야 한다. JSON 문자열 배열은 필터링이 어렵다.

---

## D-019. Prisma 6 사용 (7 아님)

**결정**: `prisma@^6` / `@prisma/client@^6` 로 고정한다.

**이유**: Prisma 7 은 생성기(`prisma-client` vs `prisma-client-js`), 출력 경로,
import 경로가 변경되어 설정이 더 복잡하다. 이 프로젝트는 `npm install && npm run dev` 만으로
동작해야 하므로 검증된 6.x 조합을 택했다. 7 로 올릴 때는
[공식 업그레이드 가이드](https://pris.ly/d/major-version-upgrade)를 따르면 된다.

**부수 결정**: npm 11.17+ 는 기본적으로 install script 를 차단한다. Prisma 엔진 바이너리
다운로드와 esbuild 설치가 이에 의존하므로 `package.json` 의 `allowScripts` 에
`prisma`, `@prisma/engines`, `@prisma/client`, `esbuild`, `unrs-resolver` 를 등록했다.

---

## D-020. "다음 10회 미리보기"는 서버 API 로 계산한다

**결정**: 폼의 미리보기를 클라이언트에서 계산하지 않고 `POST /api/recurrence/preview` 에 위임한다.
(디바운스 300ms)

**이유**:
1. 공휴일 데이터가 DB 에 있다. 클라이언트로 전부 내려보내는 것은 낭비이고, 연도가 늘면 커진다.
2. **미리보기와 실제 저장 시 생성되는 회차가 완전히 동일한 코드 경로로 계산된다.**
   둘이 갈라지면 "미리보기와 실제가 다르다"는 최악의 버그가 생긴다. 같은 순수 함수
   (`generateOccurrences`)를 쓰는 것이 이를 구조적으로 보장한다.
3. 응답에 공휴일 이름을 함께 담아 "왜 날짜가 옮겨졌는지"를 UI 가 설명할 수 있다.

---

## D-021. 완료율의 분모는 "마감일이 지난 회차"다

**결정**: 완료율 = (완료된 지난 회차) / (지난 회차 − 건너뛴 회차).
미래 회차와 건너뛴 회차는 분모에서 제외한다.

**이유**: 롤링 윈도우가 18개월치를 미리 생성하기 때문에, 미래 회차를 분모에 넣으면
완료율이 실제보다 극단적으로 낮게 나온다. 실제로 구현 중 한 업무의 완료율이
**91% 대신 27%** 로 표시되는 것을 확인했다 (33건 중 30건 완료인데 112건 전체를 분모로 삼았기 때문).

미래 회차는 "미완료"가 아니라 "아직 차례가 아닌" 것이다. 오늘 마감분도 아직 시간이
남았으므로 분모에서 제외한다. UI 에는 분모를 함께 표시해(`30/33 지난 회차 기준`) 오해를 막는다.

---

## D-022. 의존 그래프는 인라인 SVG 로 직접 그린다

**결정**: React Flow / d3 등 그래프 라이브러리를 도입하지 않고 인라인 SVG 로 렌더링한다.

**이유**:
- 사내 업무의 의존 그래프는 보통 노드 3~10개 규모다. 이 크기에서는 계층형 레이아웃
  (longest-path layering)을 직접 계산하는 것이 라이브러리 도입보다 코드가 짧고 번들도 가볍다.
- 레이아웃 계산은 이미 순수 함수(`computeLayeredLayout`)로 분리되어 테스트되어 있다.
  즉 시각화 컴포넌트는 좌표를 받아 그리기만 한다.
- 노드가 수십 개로 늘어나 팬/줌/드래그가 필요해지면 그때 라이브러리를 도입한다.
  레이아웃이 이미 분리되어 있어 교체 비용이 낮다.

---

## D-023. 필터 상태는 URL 쿼리스트링에 보관한다

**결정**: 담당자·카테고리·태그·상태·중요도·검색어를 React 상태가 아니라 URL 에 둔다.

**이유**: 새로고침·뒤로가기·링크 공유가 모두 자연스럽게 동작하고, 서버 컴포넌트가
그 값으로 바로 조회할 수 있어 클라이언트 상태 동기화 코드가 사라진다.
사내 도구에서 "이 필터 걸린 화면 좀 봐줘"라며 링크를 주고받는 일이 흔하다.

---

## D-024. 브라우저 알림은 Web Push 대신 폴링

**결정**: VAPID + Service Worker 기반 Web Push Protocol 을 쓰지 않는다.
`WEB_PUSH` 어댑터는 `NotificationLog` 에 행을 남기는 것으로 발송을 끝내고,
클라이언트가 30초마다 `/api/notifications/unread` 를 조회해 브라우저 Notification API 로 표시한다.

**이유**:
- Web Push 는 VAPID 키 생성, Service Worker 등록, 구독 정보 저장이 필요하다.
  `npm install && npm run dev` 만으로 동작해야 한다는 요구사항과 맞지 않는다.
- 로컬 단독 사용 환경에서는 브라우저가 열려 있을 때만 알림이 필요하다.

**트레이드오프**: 브라우저를 닫으면 알림이 오지 않는다. 이 경우를 위해 이메일 채널이 있다.
진짜 Web Push 가 필요해지면 이 어댑터 파일만 교체하면 되며, 스케줄러·이력·UI 는 그대로다.

---

## D-025. 회차의 마감일을 직접 바꾸면 반복 규칙에 예외로 자동 기록한다

**결정**: 회차 상세에서 `scheduledDate` 를 변경하면, 그 Task 의 `recurrenceConfig.exceptions` 에
`RESCHEDULE` 예외를 함께 기록한다.

**이유**: 그러지 않으면 다음 롤링 생성 배치가 규칙대로 날짜를 되돌려 버린다.
사용자 입장에서는 "분명히 바꿨는데 다음 날 원래대로 돌아가 있는" 최악의 버그가 된다.

예외는 원본 계산일(`originalDate`)을 키로 기록되므로(D-008) 규칙을 나중에 바꿔도
엉뚱한 회차에 적용되지 않는다.

---

## D-026. 로그인 없음, 단 다중 사용자 구조는 유지

**결정**: 인증을 구현하지 않는다. `User` 테이블과 담당자 지정은 그대로 두고,
"현재 사용자"는 `AppSetting.currentUserId` 로 선택한다.

**이유**: 로컬 PC 단독 사용이 현재 요구사항이지만, 추후 SSO 연동 가능성이 언급되었다.
담당자 개념을 지금 빼면 나중에 스키마 마이그레이션 + 데이터 백필이 필요해진다.
`User.externalId`(유니크)를 미리 두어 SSO 주체 식별자를 받을 자리를 남겨 두었다.
