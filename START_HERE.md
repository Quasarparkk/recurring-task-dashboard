# 여기서 시작하세요 (START HERE)

> 이 프로젝트를 처음 열었거나, 다른 세션·계정에서 이어받는 경우 **이 문서를 먼저** 읽으세요.
> 2분이면 충분합니다.

---

## 이게 뭔가

**사내 정기 업무 관리 대시보드**입니다. 매년·매분기·매월 반복되는 정형 업무
(연말정산 자료 취합, 분기 결산, 월 마감, 라이선스 갱신, 정기 점검 등)를
1년 단위로 조망하고 놓치지 않도록 관리합니다.

일반 To-Do 앱과의 결정적 차이는 **업무 정의(Task)와 발생 건(Occurrence)의 분리**입니다.

```
Task (템플릿)                 Occurrence (실제 실행 단위)
"매월 5일 급여 마감"    →     2026-01-05 [완료]  2026-02-05 [완료]  2026-03-05 [진행중] ...
```

완료·담당자·메모·체크리스트는 회차(Occurrence)마다 기록되고,
**반복 규칙을 수정해도 이미 지나간 회차는 바뀌지 않습니다.**
덕분에 "작년 연말정산은 누가 했고 언제 끝냈나"에 답할 수 있습니다.

---

## 바로 실행

```bash
npm install && npm run setup && npm run dev
```

`http://localhost:3000` — 시드 데이터(사내 정기 업무 23건, 공휴일 5개 연도)가 들어 있어
바로 둘러볼 수 있습니다. 외부 서비스 설정은 필요 없습니다.

작업 후에는 반드시 이 4개를 통과시키세요:

```bash
npm test && npx tsc --noEmit && npx eslint . && npm run build
```

---

## 문서 4개, 이 순서로 읽으세요

| 순서 | 문서 | 언제 읽나 |
|---|---|---|
| 1 | `START_HERE.md` (이 문서) | 지금 |
| 2 | [`HANDOFF.md`](HANDOFF.md) | **코드를 고치기 전에 반드시** |
| 3 | [`README.md`](README.md) | 기능·운영 방법을 알고 싶을 때 |
| 4 | [`DECISIONS.md`](DECISIONS.md) | "왜 이렇게 했지?" 싶을 때 |

추가로 [`data/holidays/README.md`](data/holidays/README.md) (공휴일 갱신 절차),
[`prisma/schema.prisma`](prisma/schema.prisma) (데이터 모델, 한국어 주석) 가 있습니다.

> ⚠️ **`HANDOFF.md` 의 3절 "절대 깨뜨리면 안 되는 불변식" 9개를 먼저 확인하세요.**
> 어기면 컴파일은 통과하지만 **조용히 잘못된 결과**를 내는 항목들입니다.
> 예: 순수 함수 계층에 DB를 끌어들이기, `vitest.config.ts` 의 `TZ` 강제 제거,
> 파생 상태(`BLOCKED`/`OVERDUE`)를 "성능 최적화" 명목으로 컬럼화하기.

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| 요구사항 7단계 | 전부 구현 완료 |
| 단위 테스트 | **248개 통과** — 단, 순수 함수 계층만 |
| 타입 검사 / 린트 / 빌드 | 전부 클린 |
| 서비스·API·UI 테스트 | **없음** ← 가장 큰 빈틈 |
| 인증 | 없음 (의도적, 로컬 단독 사용 전제) |

**한 문장**: 날짜 계산·반복 규칙·의존관계·알림 계획은 순수 함수로 분리해 촘촘히 테스트했고,
그 위의 DB/UI 계층은 수동 검증만 했습니다.

---

## 이어받는 분께 — 3가지만 기억하세요

### 1. 가장 큰 빈틈: 서비스 계층 테스트 부재

`occurrence-service.ts` 의 `syncOccurrencesForTask()` 는
**"규칙을 바꿔도 과거 이력이 보존된다"는 이 앱의 핵심 계약**을 지키는 코드인데
자동 테스트가 없습니다. 새 기능보다 이걸 먼저 하는 게 좋습니다.

> 근거: 개발 중 발견한 버그 6건 중, 테스트가 있는 계층에서는 1건
> (그것도 엔진이 맞고 제 기대값이 틀린 쪽)이었고, 테스트가 없는 계층에서 3건 나왔습니다.
> 상세는 `HANDOFF.md` 9절.

### 2. 조치가 필요한 데이터: 2028년 공휴일

한국천문연구원 「월력요항」 미발표 상태여서 **대체공휴일이 규칙 기반 추정치**입니다.
특히 추석과 개천절이 겹치는 **`2028-10-03`** 의 대체공휴일 일수는
정부가 1일이 아니라 2일로 지정할 여지가 남아 있습니다.

2027년 중 월력요항이 발표되면 `data/holidays/2028.json` 을 갱신하세요.
각 파일의 `_meta.verification` / `_meta.uncertain` 에 신뢰도를 기록해 뒀습니다.

> 공휴일은 **추측으로 절대 맞출 수 없습니다.** 이번에도 조사 과정에서
> 노동절이 2026년부터 법정공휴일이 된 것, 제헌절이 18년 만에 부활한 것을
> 확인해 반영했습니다. 반드시 출처를 확인하세요.

### 3. 금방 되는 개선

| 작업 | 남은 일 | 예상 |
|---|---|---|
| 다크모드 토글 | 팔레트는 완비. `ThemeProvider` + 버튼만 | 30분 |
| 회차 레벨 의존 오버라이드 | 로직·테스트 완료. API 1개 + 다이얼로그 1개 | 1~2시간 |
| 설정값 편집 UI | `setSetting()` 있음. API 1개 + 폼 | 1시간 |

전체 개선 목록(P1~P3, 13건)은 `HANDOFF.md` 6절에 우선순위순으로 있습니다.

---

## 자주 쓰는 명령

| 명령 | 동작 |
|---|---|
| `npm run dev` | 개발 서버 + 알림 스케줄러 |
| `npm test` | 단위 테스트 248개 |
| `npm run db:reset` | DB 초기화 + 시드 재생성 |
| `npm run db:studio` | Prisma Studio (DB 브라우저) |

---

## 저장소

https://github.com/Quasarparkk/recurring-task-dashboard (**Public**, 기본 브랜치 `master`)

`.env` 는 **의도적으로 커밋**되어 있습니다 (로컬 기본값뿐, SMTP 값은 전부 빈 문자열).
실제 비밀값은 gitignore된 **`.env.local`** 에 넣으세요.
자세한 내용과 커밋 이메일 변경 방법은 `HANDOFF.md` 10절 참고.
